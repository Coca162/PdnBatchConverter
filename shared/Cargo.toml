[package]
name = "pdn-conv"
edition = "2024"

[dependencies]
eyre.workspace = true
thiserror.workspace = true
directories = "6.0"
toml_edit = { version = "0.23.9" }
netcorehost = { version = "0.19.0", default-features = false, features = ["net9_0", "nethost"], optional = true}

[features]
pdn-sys = ["dep:netcorehost"]

[lints]
workspace = true